The system architecture decouples the continuous data ingestion pipeline from the user query application:Ingestion Layer (ingest.py): Continuously monitors the raw local data directory, parses heterogeneous file types, and dynamically addresses character-encoding fallbacks (e.g., Windows-1252/UTF-8 byte mismatches).Vector Space Processing: Fragments text strings into sliding windows ($1000\text{ characters}$, $200\text{ character overlap}$) to maintain structural semantic integrity, passing them through a local embedding matrix (nomic-embed-text) running inside an isolated Ollama environment.Storage Layer (db_storage/): Commits the resulting high-dimensional indices to a persistent, local disk-backed vector database powered by ChromaDB.Interface and Streaming Engine (app.py): A modern frontend interface built via Streamlit that pulls context-matching chunks instantly, applies strict internal system constraints to eliminate model hallucinations, and streams tokens to the screen in real-
